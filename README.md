# useWindowSize

Useful if you want to pass a Boolean value to a styled component, or if you want to separate components on mobile or desktop.

will write a test in the near future.

## usage
```tsx

  const { isMobile, isDesktop } = useWindowSize();

```

